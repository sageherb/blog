---
import { Moon, Sun } from "@lucide/astro";
---

<button
  type="button"
  class="theme-toggle text-text-muted hover:text-text inline-flex h-9 w-9 items-center justify-center rounded-sm transition-colors"
  data-theme-toggle
  aria-label="Toggle color theme"
>
  <Moon data-icon="moon" class="h-4 w-4" aria-hidden="true" />
  <Sun data-icon="sun" class="h-4 w-4" aria-hidden="true" />
</button>

<script>
  document.addEventListener("click", (event) => {
    const target = event.target;
    if (!(target instanceof Element)) return;
    if (!target.closest("[data-theme-toggle]")) return;

    const root = document.documentElement;
    const isDark = root.classList.toggle("dark");
    try {
      localStorage.setItem("theme", isDark ? "dark" : "light");
    } catch {
      /* private mode — session-only toggle */
    }
  });
</script>
