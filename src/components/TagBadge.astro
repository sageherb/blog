---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<a
  href={`/tags/${encodeURIComponent(tag)}`}
  class="text-accent hover:text-accent-hover text-xs transition-colors"
>
  #{tag}
</a>
